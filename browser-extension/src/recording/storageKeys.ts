/** Legacy flags (still written for compatibility until fully migrated). */
export const RECORDING_ACTIVE_KEY = "vtRecordingActive";
export const RECORDING_CONV_KEY = "vtRecordingConversationId";

/** Canonical recording session for lifecycle (B2.7 / B2.8). */
export const RECORDING_SESSION_KEY = "vtRecordingSession";
